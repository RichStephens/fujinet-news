//
// ;*** define your project wide constants here
// 
// ;*** I like to keep memory locations at top of this file
//
PMG = $5400;
LOGO_CHARSET = $5400;
CHARSET = $5800;
SPRITES = $5E00;
DISPLAY_LIST_ADDRESS1 = $5F00; 
DISPLAY_LIST_ADDRESS2 = DISPLAY_LIST_ADDRESS1 + $80;

VRAM_ADDRESS = $6000;
BUFFER_ADDRESS = $7000;


LINE_WIDTH = 40;
BLOCKSIZE = 256;

VRAM_DATE0 = VRAM_ADDRESS + (0 * BLOCKSIZE);
VRAM_HEAD0 = VRAM_ADDRESS + (0 * BLOCKSIZE) + LINE_WIDTH;
VRAM_DATE1 = VRAM_ADDRESS + (1 * BLOCKSIZE);
VRAM_HEAD1 = VRAM_ADDRESS + (1 * BLOCKSIZE) + LINE_WIDTH;
VRAM_DATE2 = VRAM_ADDRESS + (2 * BLOCKSIZE);
VRAM_HEAD2 = VRAM_ADDRESS + (2 * BLOCKSIZE) + LINE_WIDTH;
VRAM_DATE3 = VRAM_ADDRESS + (3 * BLOCKSIZE);
VRAM_HEAD3 = VRAM_ADDRESS + (3 * BLOCKSIZE) + LINE_WIDTH;
VRAM_DATE4 = VRAM_ADDRESS + (4 * BLOCKSIZE);
VRAM_HEAD4 = VRAM_ADDRESS + (4 * BLOCKSIZE) + LINE_WIDTH;
VRAM_DATE5 = VRAM_ADDRESS + (5 * BLOCKSIZE);
VRAM_HEAD5 = VRAM_ADDRESS + (5 * BLOCKSIZE) + LINE_WIDTH;
VRAM_DATE6 = VRAM_ADDRESS + (6 * BLOCKSIZE);
VRAM_HEAD6 = VRAM_ADDRESS + (6 * BLOCKSIZE) + LINE_WIDTH;

VRAM_MENU = VRAM_ADDRESS + (7 * BLOCKSIZE);
VRAM_STATUS = VRAM_MENU + LINE_WIDTH;

VRAM_CONTENT = VRAM_DATE0;

VRAM_SIZE = (8 * BLOCKSIZE);

NONE = $FF;

LONGPRESS_TIME = 50;

I_DELAY = 16;

I_QUIT = 0;
I_ENTER = 1;
I_SPACE = 2;
I_LEFT = 3;
I_RIGHT = 4;
I_UP = 5;
I_DOWN = 6;
I_TAB = 7;
I_HELP = 8;
I_RELOAD = 9;
I_IDLE = 10;

SPINNER_LEFT = 123;
VBAR_LEFT = 208;
SPINNER_TOP = 60;
PAGER_HEIGHT = 91;
PAGER_OFFSET = PMG + $180 + 17;

THEME_LEN = 11;
THEME_COUNT = 5;

APPKEY_CREATOR_ID = $b0c1;
APPKEY_APP_ID = $02;
APPKEY_CONFIG_KEY = $c0;
